{
  "unoverse": "1.0",
  "kind": "atom",
  "name": "IconButton",
  "description": "A circular icon-only button on a bordered surface: the generic small control (share, edit, refresh, more). Owns its full style — references tokens only.",
  "whenToUse": "A compact secondary action where a labeled button is too heavy. Compose via a Ref, passing the glyph with `with.icon` (or mapping it) and supplying the host's action.",
  "props": {
    "icon": {
      "type": "string",
      "default": "arrowRight"
    }
  },
  "root": {
    "type": "Button",
    "action": "click",
    "style": {
      "flex": "0 0 auto",
      "cursor": "pointer",
      "width": "8",
      "height": "8",
      "radius": "full",
      "background": "surface.base",
      "border": "subtle",
      "direction": "row",
      "align": "center",
      "justify": "center",
      "transition": "all 150ms ease",
      "hover": {
        "background": "surface.sunken"
      }
    },
    "children": [
      {
        "type": "Icon",
        "bind": {
          "name": "icon"
        },
        "style": {
          "width": "4",
          "height": "4",
          "color": "text.secondary"
        }
      }
    ]
  }
}
