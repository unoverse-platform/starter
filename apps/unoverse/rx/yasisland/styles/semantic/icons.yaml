$description: Yas Island icon additions — destination-domain semantic names → lucide glyphs, merged over the core icon set when the yasisland theme resolves.
icons:
  send:
    $value: SendHorizontal
